1. 排列数生成：n=7，(1) m=4000的序数 (2) 继续求排列 (3) 给定一个字典序排列，求下下个
2. 波利亚定理：圆圈三个点(三点构成等边三角形)，六种颜色填充，(1) 只允许旋转；(2) 旋转加翻转
3. 齐次递推关系：一元可买两样，两元可买三样东西，(1) 求 n 元的购买方案的递推关系；(2) 求解递推公式；(3) 求解 $a_n/a_{n-1}$ 极限
4. 鸽巢原理：男生女生总共165人，排成5行33列，至少有2列性别序列一致
5. 容斥原理：已知2014的所有质因数是2，19，53。 现给 $\frac{1}{2014}$, $\frac{2}{2013}$, $\frac{3}{2014}$, ... , $\frac{2013}{2014}$ 约分。求
(1) 约分后能保持分母2014不变的分数的总数 (2)约分后能保持分母2014不变的分数之和
6. 排列组合：从0到8 **(含0和8)** 中选择6个数填到如下2*3方格中，要求满足：左边数 < 右边数，上边数 < 下边数，有多少种填法

|   |  |  |
| --- | --- | --- |
|  |  |  |

7. 非齐次递推关系：橙绿汉诺塔(奇偶汉诺塔改名) (1) 写 Bi-Hanoi 填空；(2) 列出递推关系 (线性常系数非齐次)；(3) 写出非齐次的一个特解；(4) 求解非齐次的递推公式
(1) 填空题：
``` c
void Bi-Hanoi(int n,char source,char orangetarget,char yellowtarget)
{
  if (n==0) ;
  else if (n==1)
    printf(“move disk 1 from %c to %c”, source, yellowtarget);
  else if (n==2){
    printf(“move disk 1 from %c to %c”, source, yellowtarget);
    printf(“move disk 2 from %c to %c”, source, orangetarget);
  }
  else { 
  char temp = orangetarget, target = yellowtarget;
  if (n%2!=0)
  {
    temp = yellowtarget, target = orangetarget;} 
    Hanoi(n-1, source , temp, target);
    printf(“move disk %d from %c to %c”,n, source, temp);
    Hanoi(___①___, ___②___, temp, ___③___);
    printf(“move disk %d from %c to %c”,n-2, target, temp);
    Bi-Hanoi(___④___, source，___⑤___, ___⑥___);
  }
}
```
